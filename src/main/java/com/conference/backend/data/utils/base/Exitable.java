package com.conference.backend.data.utils.base;

/**
 * The {@code Exitable} interface should be implemented
 * for classes whose instances are intended to be ended.
 */
public interface Exitable {
    /**
     * Should be implemented if a program is meant to quit.
     */
    void quit();
}
